<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysNoticeMapper">
    
    <resultMap type="SysNotice" id="SysNoticeResult">
        <result property="noticeId"      column="notice_id"      />
        <result property="noticeTitle"    column="notice_title"    />
        <result property="noticeType"    column="notice_type"    />
        <result property="noticeContent"  column="notice_content"  />
        <result property="status"        column="status"        />
        <result property="createBy"      column="create_by"      />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"        column="remark"        />
    </resultMap>

    <resultMap type="SysNotice" id="SysNoticeWithReadStatusResult">
        <result property="noticeId"      column="notice_id"      />
        <result property="noticeTitle"    column="notice_title"    />
        <result property="noticeType"    column="notice_type"    />
        <result property="noticeContent"  column="notice_content"  />
        <result property="status"        column="status"        />
        <result property="createBy"      column="create_by"      />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"        column="remark"        />
        <result property="isRead"        column="is_read"        />
    </resultMap>
    
    <sql id="selectNoticeVo">
        select notice_id, notice_title, notice_type, notice_content, status, create_by, create_time, update_by, update_time, remark 
        from sys_notice
    </sql>
    
    <!-- 查询公告信息 -->
    <select id="selectNoticeById" parameterType="Long" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        where notice_id = #{noticeId}
    </select>
    
    <!-- 查询公告列表 -->
    <select id="selectNoticeList" parameterType="SysNotice" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        <where>
            <if test="noticeTitle != null and noticeTitle != ''">
                AND notice_title like concat('%', #{noticeTitle}, '%')
            </if>
            <if test="noticeType != null and noticeType != ''">
                AND notice_type = #{noticeType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="createBy != null and createBy != ''">
                AND create_by like concat('%', #{createBy}, '%')
            </if>
            <if test="noticeIds != null and noticeIds.size() > 0">
                AND notice_id IN
                <foreach collection="noticeIds" item="noticeId" open="(" separator="," close=")">
                    #{noticeId}
                </foreach>
            </if>
        </where>
        order by create_time desc
    </select>
    
    <!-- 新增公告 -->
    <insert id="insertNotice" parameterType="SysNotice" useGeneratedKeys="true" keyProperty="noticeId">
        insert into sys_notice (
            <if test="noticeTitle != null and noticeTitle != ''">notice_title,</if>
            <if test="noticeType != null and noticeType != ''">notice_type,</if>
            <if test="noticeContent != null and noticeContent != ''">notice_content,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="noticeTitle != null and noticeTitle != ''">#{noticeTitle},</if>
            <if test="noticeType != null and noticeType != ''">#{noticeType},</if>
            <if test="noticeContent != null and noticeContent != ''">#{noticeContent},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>
    
    <!-- 修改公告 -->
    <update id="updateNotice" parameterType="SysNotice">
        update sys_notice
        <set>
            <if test="noticeTitle != null and noticeTitle != ''">notice_title = #{noticeTitle},</if>
            <if test="noticeType != null and noticeType != ''">notice_type = #{noticeType},</if>
            <if test="noticeContent != null and noticeContent != ''">notice_content = #{noticeContent},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where notice_id = #{noticeId}
    </update>
    
    <!-- 删除公告 -->
    <delete id="deleteNoticeById" parameterType="Long">
        delete from sys_notice where notice_id = #{noticeId}
    </delete>
    
    <!-- 批量删除公告 -->
    <delete id="deleteNoticeByIds" parameterType="Long">
        delete from sys_notice where notice_id in 
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>
    
    <!-- 查询用户公告列表（带阅读状态） -->
    <select id="selectUserNoticeListWithReadStatus" resultMap="SysNoticeWithReadStatusResult">
        SELECT 
            n.notice_id, n.notice_title, n.notice_type, n.notice_content, n.status, 
            n.create_by, n.create_time, n.update_by, n.update_time, n.remark,
            CASE WHEN r.read_id IS NOT NULL THEN 1 ELSE 0 END as is_read
        FROM sys_notice n
        LEFT JOIN sys_notice_read r ON n.notice_id = r.notice_id AND r.user_id = #{userId}
        <where>
            <if test="notice.noticeTitle != null and notice.noticeTitle != ''">
                AND n.notice_title like concat('%', #{notice.noticeTitle}, '%')
            </if>
            <if test="notice.noticeType != null and notice.noticeType != ''">
                AND n.notice_type = #{notice.noticeType}
            </if>
            <if test="notice.status != null and notice.status != ''">
                AND n.status = #{notice.status}
            </if>
            <if test="notice.createBy != null and notice.createBy != ''">
                AND n.create_by like concat('%', #{notice.createBy}, '%')
            </if>
            <if test="notice.noticeIds != null and notice.noticeIds.size() > 0">
                AND n.notice_id IN
                <foreach collection="notice.noticeIds" item="noticeId" open="(" separator="," close=")">
                    #{noticeId}
                </foreach>
            </if>
        </where>
        ORDER BY n.create_time DESC
    </select>
    
</mapper>