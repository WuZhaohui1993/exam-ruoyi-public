<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.exam.question.mapper.ExamQuestionCategoryMapper">

	<resultMap type="ExamQuestionCategory" id="ExamQuestionCategoryResult">
		<id     property="id"             column="id"            />
		<result property="categoryName"   column="category_name" />
		<result property="parentId"       column="parent_id"     />
		<result property="orderNum"       column="order_num"     />
		<result property="status"         column="status"        />
		<result property="createBy"       column="create_by"     />
		<result property="createTime"     column="create_time"   />
		<result property="updateBy"       column="update_by"     />
		<result property="updateTime"     column="update_time"   />
		<result property="remark"         column="remark"        />
	</resultMap>
	
	<sql id="selectCategoryVo">
        select id, category_name, parent_id, order_num, status, create_by, create_time, update_by, update_time, remark
		from exam_question_category
    </sql>
	
	<select id="selectCategoryList" parameterType="ExamQuestionCategory" resultMap="ExamQuestionCategoryResult">
	    <include refid="selectCategoryVo"/>
		<where>
			<if test="categoryName != null and categoryName != ''">
				AND category_name like concat('%', #{categoryName}, '%')
			</if>
			<if test="parentId != null">
				AND parent_id = #{parentId}
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by parent_id, order_num
	</select>
	
	<select id="selectCategoryAll" resultMap="ExamQuestionCategoryResult">
		<include refid="selectCategoryVo"/>
		where status = '0'
		order by parent_id, order_num
	</select>
	
	<select id="selectCategoryById" parameterType="Long" resultMap="ExamQuestionCategoryResult">
		<include refid="selectCategoryVo"/>
		where id = #{id}
	</select>
	
	<select id="selectChildrenCategoryCount" parameterType="Long" resultType="int">
		select count(1) from exam_question_category where parent_id = #{parentId}
	</select>
	
	<select id="checkCategoryNameUnique" resultMap="ExamQuestionCategoryResult">
		<include refid="selectCategoryVo"/>
		where category_name = #{categoryName} and parent_id = #{parentId} limit 1
	</select>
	
	<select id="selectQuestionCountByCategoryId" parameterType="Long" resultType="int">
		select count(1) from exam_question where category_id = #{categoryId}
	</select>
	
	<insert id="insertCategory" parameterType="ExamQuestionCategory" useGeneratedKeys="true" keyProperty="id">
 		insert into exam_question_category(
 			<if test="categoryName != null and categoryName != ''">category_name,</if>
 			<if test="parentId != null">parent_id,</if>
 			<if test="orderNum != null">order_num,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null and remark != ''">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="categoryName != null and categoryName != ''">#{categoryName},</if>
 			<if test="parentId != null">#{parentId},</if>
 			<if test="orderNum != null">#{orderNum},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null and remark != ''">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>
	
	<update id="updateCategory" parameterType="ExamQuestionCategory">
 		update exam_question_category
 		<set>
 			<if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
 			<if test="parentId != null">parent_id = #{parentId},</if>
 			<if test="orderNum != null">order_num = #{orderNum},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where id = #{id}
	</update>
 	
	<delete id="deleteCategoryById" parameterType="Long">
		delete from exam_question_category where id = #{id}
	</delete>
	
	<delete id="deleteCategoryByIds" parameterType="Long">
 		delete from exam_question_category where id in
 		<foreach collection="array" item="id" open="(" separator="," close=")">
 			#{id}
        </foreach> 
 	</delete>

    <select id="selectCategoryByName" parameterType="String" resultMap="ExamQuestionCategoryResult">
        <include refid="selectCategoryVo"/>
        where category_name = #{categoryName} and status = '0'
        limit 1
    </select>

</mapper> 