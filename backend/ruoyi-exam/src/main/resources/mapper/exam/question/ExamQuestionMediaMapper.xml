<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.exam.question.mapper.ExamQuestionMediaMapper">

    <resultMap type="ExamQuestionMedia" id="ExamQuestionMediaResult">
        <id     property="id"              column="id"              />
        <result property="questionId"      column="question_id"     />
        <result property="mediaType"       column="media_type"      />
        <result property="fileUrl"         column="file_url"        />
        <result property="thumbnailUrl"    column="thumbnail_url"   />
        <result property="fileName"        column="file_name"       />
        <result property="fileSize"        column="file_size"       />
        <result property="duration"        column="duration"        />
        <result property="sortOrder"       column="sort_order"      />
        <result property="displayPosition" column="display_position"/>
        <result property="status"          column="status"          />
        <result property="createBy"        column="create_by"       />
        <result property="createTime"      column="create_time"     />
        <result property="updateBy"        column="update_by"       />
        <result property="updateTime"      column="update_time"     />
        <result property="remark"          column="remark"          />
    </resultMap>

    <sql id="selectQuestionMediaVo">
        select id, question_id, media_type, file_url, thumbnail_url, file_name,
               file_size, duration, sort_order, display_position, status,
               create_by, create_time, update_by, update_time, remark
        from exam_question_media
    </sql>

    <select id="selectQuestionMediaByQuestionId" parameterType="Long" resultMap="ExamQuestionMediaResult">
        <include refid="selectQuestionMediaVo"/>
        where question_id = #{questionId} and status = '0'
        order by sort_order asc, id asc
    </select>

    <select id="selectQuestionMediaByQuestionIds" resultMap="ExamQuestionMediaResult">
        <include refid="selectQuestionMediaVo"/>
        where status = '0'
          and question_id in
        <foreach collection="questionIds" item="questionId" open="(" separator="," close=")">
            #{questionId}
        </foreach>
        order by question_id asc, sort_order asc, id asc
    </select>

    <insert id="insertQuestionMedia" parameterType="ExamQuestionMedia" useGeneratedKeys="true" keyProperty="id">
        insert into exam_question_media(
            question_id, media_type, file_url, thumbnail_url, file_name,
            file_size, duration, sort_order, display_position, status,
            create_by, create_time, remark
        ) values (
            #{questionId}, #{mediaType}, #{fileUrl}, #{thumbnailUrl}, #{fileName},
            #{fileSize}, #{duration}, #{sortOrder}, #{displayPosition}, #{status},
            #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <insert id="batchInsertQuestionMedia">
        insert into exam_question_media(
            question_id, media_type, file_url, thumbnail_url, file_name,
            file_size, duration, sort_order, display_position, status,
            create_by, create_time, remark
        ) values
        <foreach collection="list" item="item" separator=",">
            (
                #{item.questionId}, #{item.mediaType}, #{item.fileUrl}, #{item.thumbnailUrl}, #{item.fileName},
                #{item.fileSize}, #{item.duration}, #{item.sortOrder}, #{item.displayPosition}, #{item.status},
                #{item.createBy}, sysdate(), #{item.remark}
            )
        </foreach>
    </insert>

    <delete id="deleteQuestionMediaByQuestionId" parameterType="Long">
        delete from exam_question_media where question_id = #{questionId}
    </delete>

    <delete id="deleteQuestionMediaByQuestionIds" parameterType="Long">
        delete from exam_question_media where question_id in
        <foreach collection="array" item="questionId" open="(" separator="," close=")">
            #{questionId}
        </foreach>
    </delete>

</mapper>
