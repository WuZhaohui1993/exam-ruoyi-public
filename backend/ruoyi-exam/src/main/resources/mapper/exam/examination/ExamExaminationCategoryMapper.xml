<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.exam.examination.mapper.ExamExaminationCategoryMapper">
    
    <resultMap type="ExamExaminationCategory" id="ExamExaminationCategoryResult">
        <result property="id"           column="id"           />
        <result property="categoryName" column="category_name" />
        <result property="parentId"     column="parent_id"     />
        <result property="orderNum"     column="order_num"     />
        <result property="status"       column="status"       />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"       />
    </resultMap>

    <sql id="selectExamExaminationCategoryVo">
        select id, category_name, parent_id, order_num, status, create_by, create_time, update_by, update_time, remark 
        from exam_examination_category
    </sql>

    <select id="selectExamExaminationCategoryList" parameterType="ExamExaminationCategory" resultMap="ExamExaminationCategoryResult">
        <include refid="selectExamExaminationCategoryVo"/>
        <where>  
            <if test="categoryName != null  and categoryName != ''"> and category_name like concat('%', #{categoryName}, '%')</if>
            <if test="parentId != null "> and parent_id = #{parentId}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
        order by parent_id, order_num
    </select>
    
    <select id="selectExamExaminationCategoryById" parameterType="Long" resultMap="ExamExaminationCategoryResult">
        <include refid="selectExamExaminationCategoryVo"/>
        where id = #{id}
    </select>

    <select id="selectExamExaminationCategoryListByRoleId" resultType="Long">
        select c.id
        from exam_examination_category c
            left join sys_role_dept rd on c.id = rd.dept_id
        where rd.role_id = #{roleId}
            <if test="categoryCheckStrictly">
              and c.id not in (select c.parent_id from exam_examination_category c inner join sys_role_dept rd on c.id = rd.dept_id and rd.role_id = #{roleId})
            </if>
        order by c.parent_id, c.order_num
    </select>

    <select id="selectExamExaminationCategorysByUserId" parameterType="Long" resultMap="ExamExaminationCategoryResult">
        select distinct c.id, c.category_name, c.parent_id, c.order_num, c.status, c.create_by, c.create_time, c.update_by, c.update_time, c.remark
        from exam_examination_category c
            left join sys_role_dept rd on c.id = rd.dept_id
            left join sys_user_role ur on rd.role_id = ur.role_id
            left join sys_role ro on ur.role_id = ro.role_id
        where c.status = '0' and ro.status = '0' and ur.user_id = #{userId}
        order by c.parent_id, c.order_num
    </select>

    <select id="checkExamExaminationCategoryExistUser" parameterType="Long" resultType="int">
        select count(1) from exam_examination where category_id = #{categoryId} and del_flag = '0'
    </select>

    <select id="checkCategoryNameUnique" resultMap="ExamExaminationCategoryResult">
        <include refid="selectExamExaminationCategoryVo"/>
        where category_name=#{categoryName} and parent_id = #{parentId} limit 1
    </select>

    <select id="selectChildrenCategoryById" parameterType="Long" resultType="int">
        select count(1) from exam_examination_category where parent_id = #{parentId}
    </select>

    <select id="selectChildrenCategoryByParentId" parameterType="Long" resultMap="ExamExaminationCategoryResult">
        <include refid="selectExamExaminationCategoryVo"/>
        where parent_id = #{parentId} and status = '0'
        order by order_num
    </select>

    <insert id="insertExamExaminationCategory" parameterType="ExamExaminationCategory" useGeneratedKeys="true" keyProperty="id">
        insert into exam_examination_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryName != null and categoryName != ''">category_name,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="orderNum != null">order_num,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            create_time,
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryName != null and categoryName != ''">#{categoryName},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="orderNum != null">#{orderNum},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate(),
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateExamExaminationCategory" parameterType="ExamExaminationCategory">
        update exam_examination_category
        <trim prefix="SET" suffixOverrides=",">
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate(),
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteExamExaminationCategoryById" parameterType="Long">
        delete from exam_examination_category where id = #{id}
    </delete>

    <delete id="deleteExamExaminationCategoryByIds" parameterType="String">
        delete from exam_examination_category where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 检查分类下是否存在考试 -->
    <select id="checkCategoryExistExamination" parameterType="Long" resultType="int">
        select count(*) from exam_examination where category_id = #{categoryId}
    </select>

</mapper> 